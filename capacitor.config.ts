import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ai.softage.missiongermany',
  appName: 'Mission Germany',
  webDir: 'out',
};

export default config;
